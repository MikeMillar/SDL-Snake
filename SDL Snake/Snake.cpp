#include "Snake.h"
#include "Screen.h"

using namespace std;

Snake::Snake(): m_x(400), m_y(400), m_tailSize(0), headWidth(9), headHeight(9), tailWidth(5), tailHeight(5) {}

Snake::~Snake() {}

void Snake::init() {
	
}

void Snake::drawSnake() {
	
	

	if (m_tailSize > 0) {
		// Draw tail
	}
}


